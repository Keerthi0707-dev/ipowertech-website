import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Brand Colors (Red, White, Black Theme)
  static const Color primary = Colors.red; // Red for highlights
  static const Color accent = Colors.white; // White for buttons/text
  static const Color background = Color(0xFF0F0F0F); // Dark background
  static const Color black = Colors.black;
  static const Color white = Colors.white;

  // 🧾 Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // 🪟 UI Component Colors
  static const Color card = Color(0xFF1E1E1E); // Dark grey for cards
  static const Color border = Colors.redAccent;
}
